# POLYMORPHISM-Traffic-Management-System
The concept demonstrate 
Inheritance where the child classes inherit from the parent class
Method Overriding where each child class provides its own implementation of activate()
Object stored in a list where the devices list has one object from each class
Polymorphism where the loop calls activate() on each object without checking its type
